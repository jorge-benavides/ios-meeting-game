//: [Previous](@previous)

import SwiftUI
import PlaygroundSupport

// uikit playground preview

struct view: View {
    var body: some View {
        Text("hello world")
            .padding(40)
    }
}

PlaygroundPage.current.setLiveView(view())

//: [Next](@next)
